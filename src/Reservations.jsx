/* Imports */
/* Defenitions */
/* First Function */
export default function Reservations() {}
/*   const [books, SetBooks] = useState([]);
  useEffect(() => {
    async function makeBooks() {
      const data = await getBooks();
      SetBooks(data);
    }
    makeBooks();
    console.log(books);
  }, []);
  books.filter((book) => book.available === "true");
  console.log(onlyAvailible);
  return (
    <>
      <p>Home Page</p>
      <div>
        <ul className="reservationsCard">
          {books.map((book) => (
            <li key={book.id}>
              <Link key={book.id} to={`/${book.id}`}>
                <h3>{book.title}</h3>
              </Link>
              — <h5>{book.author}</h5> <img src={book.coverimage} />
              <p>{book.description}</p>
              {book.available === "true" && <button>+</button>}
              <button>-</button>
            </li>
          ))}
        </ul>
      </div>
    </>
  );
} */
